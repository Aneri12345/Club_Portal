import React, {Component} from 'react';

class ServerError extends Component {
    render() {
        return(
            <h1>There is something wrong with the server.... Please try again later.</h1>
        );
    }
}

export default ServerError;